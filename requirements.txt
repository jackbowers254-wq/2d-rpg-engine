# ---------------------------------------------------------------------------
# 2D RPG Engine - dependencies
# ---------------------------------------------------------------------------
# Keep this list MINIMAL and documented. The engine is built so that the only
# hard runtime dependency is pygame. Everything else (map format, data files)
# uses the Python standard library (json).
#
# Install with:   pip install -r requirements.txt
# ---------------------------------------------------------------------------

pygame>=2.5,<3        # Windowing, input, 2D rendering, audio mixer, fonts.
