"""
Demo GAME package.

Everything here is specific to the demo game built ON TOP of the reusable
``engine`` package: the shared game state, the entity factory that reads
``data/entities``, and the concrete scenes (title, overworld, dialogue, battle,
pause, inventory). To start a new game you keep ``engine/`` and rewrite (or copy
and edit) this package + the ``data/`` and ``config/`` folders.
"""
